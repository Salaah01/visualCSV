/**Imports all corefunctions only to export them.
 * Making calling of corefunctions easier and so that it behaves like its own
 * package.
 */

export { updateObject } from "./updateObject";
export { deepCopyGrid } from "./deepCopyGrid";
